# Comment Templates
- **Nitpick:** Used for minor style issues that don't block. "Nit: Consider using `.map` here."
- **Question:** Used to understand logic. "Question: Why are we returning null here instead of throwing an error?"
- **Blocker:** Used when logic is fundamentally flawed or a security risk. "Blocker: This SQL string concatenation is vulnerable to injection."
